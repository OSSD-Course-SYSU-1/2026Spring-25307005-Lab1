 # 计算器新增功能说明

## 功能概述

本次更新为计算器应用添加了三个重要功能：
1. **历史记录查询功能**
2. **分数形式输入和输出**
3. **分数和小数输出切换功能**

---

## 1. 历史记录查询功能

### 功能特点
- 自动保存每次计算的表达式和结果
- 最多保存100条历史记录
- 显示计算时间戳
- 支持快速查看和使用历史记录

### 使用方法
1. 点击顶部的"历史"按钮，展开历史记录面板
2. 历史记录按时间倒序排列（最新的在最上面）
3. 点击任意历史记录项，可快速将该记录应用到当前计算
4. 点击"清空"按钮可删除所有历史记录

### 技术实现
- 新增文件：`entry/src/main/ets/common/util/HistoryManager.ets`
- 历史记录数据结构：
  ```typescript
  interface HistoryItem {
    expression: string;  // 原始表达式
    result: string;      // 计算结果
    timestamp: number;   // 时间戳
  }
  ```

---

## 2. 分数形式输入和输出

### 功能特点
- 支持直接输入分数格式（如 `3/4`、`-5/6`）
- 支持带分数格式（如 `1 1/2`）
- 自动化简分数到最简形式
- 支持分数的加减乘除运算
- 支持分数的幂运算

### 使用方法

#### 方法一：直接输入分数
1. 在科学模式下，使用数字键盘输入分子
2. 点击分数输入按钮 "a/b"
3. 继续输入分母
4. 例如：输入 `3` → 点击 `a/b` → 输入 `4`，得到 `3/4`

#### 方法二：输入带分数
1. 输入整数部分
2. 点击 "a b/c" 按钮（添加空格）
3. 输入分子
4. 点击 "a/b" 按钮
5. 输入分母
6. 例如：输入 `1` → 点击 `a b/c` → 输入 `1` → 点击 `a/b` → 输入 `2`，得到 `1 1/2`

### 分数计算示例
- `1/2 + 1/3 = 5/6`
- `3/4 × 2/3 = 1/2`
- `2/3 ÷ 1/2 = 4/3`
- `(1/2)^2 = 1/4`

### 技术实现
- 新增文件：`entry/src/main/ets/common/util/FractionUtil.ets`
- 核心功能：
  - 最大公约数（GCD）计算
  - 最小公倍数（LCM）计算
  - 分数化简
  - 分数四则运算
  - 小数转分数（使用连分数算法）
  - 分数转小数

---

## 3. 分数和小数输出切换功能

### 功能特点
- 一键切换分数/小数显示模式
- 实时转换当前计算结果
- 智能判断是否显示分数形式

### 使用方法
1. 点击顶部的"分数模式"/"小数模式"按钮进行切换
2. **分数模式**：计算结果会自动转换为分数形式
   - 例如：`0.75` 显示为 `3/4`
   - 例如：`0.333...` 显示为 `1/3`
3. **小数模式**：分数结果会转换为小数
   - 例如：`1/3` 显示为 `0.3333333333333333`

### 智能显示规则
- 只有当分母 ≤ 1000 时才显示分数形式
- 避免显示过于复杂的分数（如 `123/4567`）
- 无理数（如 π、√2）保持小数形式

### 技术实现
- 在 `HomePage.ets` 中添加：
  - `isFractionMode` 状态变量
  - `convertToFraction()` 方法：小数转分数
  - `updateDisplayFormat()` 方法：更新显示格式
- 在 `CalculateUtil.ets` 中添加：
  - `isFraction()` 方法：判断是否为分数格式
  - `parseValue()` 方法：解析分数或小数
  - `numberToFraction()` 方法：数字转分数

---

## UI 界面变化

### 顶部控制栏
新增三个控制按钮（从左到右）：
1. **历史按钮**（灰色）：点击展开/收起历史记录面板
2. **分数模式按钮**（青色/红色）：
   - 青色：当前为小数模式，点击切换到分数模式
   - 红色：当前为分数模式，点击切换到小数模式
3. **科学模式按钮**（蓝色）：切换标准/科学计算模式

### 历史记录面板
- 显示位置：在顶部控制栏下方
- 包含内容：
  - 标题栏："计算历史" + "清空"按钮
  - 历史记录列表（或"暂无历史记录"提示）
  - 每条记录显示：表达式 = 结果 + 时间戳

### 科学模式新增按钮
在科学计算按钮下方新增：
- **分数输入区域**：
  - "a/b" 按钮：输入分数分隔符
  - "a b/c" 按钮：输入带分数分隔符

---

## 文件结构

### 新增文件
```
entry/src/main/ets/common/util/
├── HistoryManager.ets    # 历史记录管理
└── FractionUtil.ets      # 分数计算工具
```

### 修改文件
```
entry/src/main/ets/
├── pages/
│   └── HomePage.ets      # 主页面（添加UI和功能）
└── common/util/
    └── CalculateUtil.ets # 计算工具（支持分数）
```

---

## 使用示例

### 示例1：分数计算
1. 切换到科学模式
2. 输入 `1` → 点击 `a/b` → 输入 `2`（得到 1/2）
3. 点击 `+`
4. 输入 `1` → 点击 `a/b` → 输入 `3`（得到 1/3）
5. 点击 `=`
6. 结果显示：`5/6`（分数模式）或 `0.8333333333333333`（小数模式）

### 示例2：查看历史
1. 进行几次计算
2. 点击"历史"按钮
3. 查看所有计算记录
4. 点击某条记录可快速使用

### 示例3：模式切换
1. 计算 `1 ÷ 3`
2. 小数模式显示：`0.3333333333333333`
3. 点击"分数模式"按钮
4. 显示自动变为：`1/3`

---

## 技术亮点

1. **精确计算**：使用分数计算避免浮点数精度问题
2. **智能转换**：连分数算法实现精确的小数转分数
3. **用户友好**：自动化简分数，显示最简形式
4. **历史管理**：完整的增删查功能，支持快速复用
5. **无缝集成**：与现有科学计算功能完美配合

---

## 注意事项

1. 分数输入需要在**科学模式**下使用
2. 分数分母不能为0
3. 历史记录最多保存100条，超出后自动删除最旧的记录
4. 过于复杂的小数（分母>1000）不会转换为分数显示
5. 历史记录在应用关闭后不会保存（如需持久化，需添加本地存储功能）

---

## 后续优化建议

1. **持久化存储**：将历史记录保存到本地存储
2. **更多分数操作**：支持分数的比较、取整等操作
3. **历史搜索**：添加搜索功能快速查找历史记录
4. **分数化简选项**：可选择是否显示带分数形式
5. **精度设置**：允许用户自定义分数分母的最大值
