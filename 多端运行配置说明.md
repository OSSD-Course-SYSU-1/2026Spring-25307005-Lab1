# HarmonyOS 多端运行配置说明

## 问题原因

修改了 `build-profile.json5` 后,DevEco Studio 需要重新同步项目配置才能识别新的 product 配置。

## 解决步骤

### 步骤1: 同步项目配置(必须)

在 DevEco Studio 中执行以下操作:

1. 点击菜单 `File` → `Sync Project with Build Profiles`
2. 或者点击工具栏的同步图标(🔄)
3. 等待同步完成

### 步骤2: 选择运行配置

同步完成后,在 DevEco Studio 中:

1. 点击顶部工具栏的运行配置下拉菜单
2. 选择要运行的配置:
   - **entry** - 运行手机版本(推荐)
   - **phone** - 运行手机版本(带依赖)
   - **tablet** - 运行平板版本
   - **pc** - 运行PC版本

### 步骤3: 选择设备

1. 在设备下拉菜单中选择对应的设备:
   - 运行 **entry** 或 **phone** 时,选择手机设备或模拟器
   - 运行 **tablet** 时,选择平板设备或模拟器
   - 运行 **pc** 时,选择PC设备或模拟器

### 步骤4: 点击运行

现在运行按钮应该可以点击了,点击绿色的运行按钮(▶️)即可运行应用。

## 如果还是不能运行

### 方案A: 清理并重新构建

```bash
# 清理构建
"C:\DevEco Studio\tools\node\node.exe" "C:\DevEco Studio\tools\hvigor\bin\hvigorw.js" clean

# 重新构建
build-multi-platform.bat
```

### 方案B: 刷新IDE缓存

1. 点击 `File` → `Invalidate Caches / Restart`
2. 选择 `Invalidate and Restart`
3. 等待IDE重启完成
4. 重新执行步骤1-4

### 方案C: 手动创建运行配置

1. 点击 `Run` → `Edit Configurations`
2. 点击 `+` 号,选择 `OpenHarmony App`
3. 配置如下:
   - **Name**: entry (或其他名称)
   - **Module**: entry
   - **Product**: phone
   - **Launch Ability**: Default Ability
4. 点击 `OK` 保存

## Product 配置说明

当前项目配置了三个 product:

- **phone** - 手机平台
  - 模块: entry, phone, common, calculator
  - 设备类型: phone

- **tablet** - 平板平台
  - 模块: tablet, common, calculator
  - 设备类型: tablet

- **pc** - PC平台
  - 模块: pc, common, calculator
  - 设备类型: 2in1

## 常见问题

### Q: 为什么运行按钮是灰色的?

A: 可能的原因:
1. 项目配置未同步 - 执行步骤1
2. 没有选择设备 - 执行步骤3
3. 构建配置有错误 - 查看Build Output窗口

### Q: 构建失败怎么办?

A: 
1. 查看错误信息
2. 使用 `build-multi-platform.bat` 脚本构建
3. 检查设备类型是否匹配

### Q: 如何切换不同的平台?

A: 
1. 在运行配置下拉菜单中选择对应的配置
2. 确保选择了正确的设备类型
3. 点击运行

## 构建命令参考

### 手机版本
```bash
"C:\DevEco Studio\tools\node\node.exe" "C:\DevEco Studio\tools\hvigor\bin\hvigorw.js" --mode module -p module=entry@default -p product=phone -p requiredDeviceType=phone assembleHap --analyze=normal --parallel --incremental --daemon
```

### 平板版本
```bash
"C:\DevEco Studio\tools\node\node.exe" "C:\DevEco Studio\tools\hvigor\bin\hvigorw.js" --mode module -p module=tablet@default,common@default,calculator@default -p product=tablet -p requiredDeviceType=tablet assembleHap assembleHsp --analyze=normal --parallel --incremental --daemon
```

### PC版本
```bash
"C:\DevEco Studio\tools\node\node.exe" "C:\DevEco Studio\tools\hvigor\bin\hvigorw.js" --mode module -p module=pc@default,common@default,calculator@default -p product=pc -p requiredDeviceType=2in1 assembleHap assembleHsp --analyze=normal --parallel --incremental --daemon
```
