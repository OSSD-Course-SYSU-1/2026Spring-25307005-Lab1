# SimpleCalculator 工程文件描述文档

## 一、项目概述

### 1.1 基本信息
- **项目名称**: SimpleCalculator (简易计算器)
- **项目类型**: HarmonyOS 应用程序
- **开发语言**: ArkTS (TypeScript超集，华为鸿蒙系统开发语言)
- **项目描述**: 基于HarmonyOS开发的简易计算器应用，支持加减乘除混合运算，具有实时计算结果显示功能
- **开源协议**: Apache License 2.0

### 1.2 开发环境要求
- **HarmonyOS系统**: HarmonyOS 5.0.5 Release及以上
- **DevEco Studio版本**: DevEco Studio 6.0.2 Release及以上
- **HarmonyOS SDK版本**: HarmonyOS 6.0.2 Release SDK及以上
- **支持设备**: 华为手机

---

## 二、工程目录结构

```
SimpleCalculator-master/
├── .hvigor/                          # Hvigor构建工具配置目录
│   └── dependencyMap/                # 依赖映射配置
├── .idea/                            # IDE配置目录
├── AppScope/                         # 应用全局配置
│   ├── app.json5                     # 应用配置文件
│   └── resources/                    # 全局资源
│       ├── base/element/string.json  # 字符串资源
│       └── base/media/app_icon.png   # 应用图标
├── entry/                            # 主模块目录
│   ├── src/main/                     # 源代码主目录
│   │   ├── ets/                      # ArkTS源代码
│   │   │   ├── common/               # 公共模块
│   │   │   │   ├── constants/        # 常量定义
│   │   │   │   │   └── CommonConstants.ets
│   │   │   │   └── util/             # 工具类
│   │   │   │       ├── CalculateUtil.ets
│   │   │   │       ├── CheckEmptyUtil.ets
│   │   │   │       └── Logger.ets
│   │   │   ├── entryability/         # 应用入口
│   │   │   │   └── EntryAbility.ts
│   │   │   ├── pages/                # 页面
│   │   │   │   └── HomePage.ets
│   │   │   └── viewmodel/            # 视图模型
│   │   │       ├── PressKeysItem.ets
│   │   │       └── PresskeysViewModel.ets
│   │   ├── resources/                # 资源文件
│   │   │   ├── base/                 # 基础资源
│   │   │   │   ├── element/          # 元素资源
│   │   │   │   │   ├── color.json    # 颜色定义
│   │   │   │   │   ├── float.json    # 尺寸定义
│   │   │   │   │   └── string.json  # 字符串定义
│   │   │   │   ├── media/            # 媒体资源(图标)
│   │   │   │   └── profile/          # 配置文件
│   │   │   │       └── main_pages.json
│   │   │   ├── en_US/                # 英文资源
│   │   │   └── zh_CN/                # 中文资源
│   │   └── module.json5              # 模块配置
│   ├── build/                        # 构建输出目录
│   ├── build-profile.json5           # 模块构建配置
│   ├── hvigorfile.ts                 # Hvigor构建脚本
│   └── oh-package.json5              # 模块包配置
├── hvigor/                           # Hvigor构建工具
│   └── hvigor-config.json5
├── screenshots/                      # 截图目录
│   └── device/                       # 设备截图
├── build-profile.json5               # 项目构建配置
├── hvigorfile.ts                     # 项目构建脚本
├── oh-package.json5                  # 项目包配置
├── LICENSE                           # 许可证文件
├── README.md                         # 项目说明文档
└── README.en.md                      # 英文说明文档
```

---

## 三、核心源代码文件说明

### 3.1 应用入口层

#### EntryAbility.ts
**文件路径**: `entry/src/main/ets/entryability/EntryAbility.ts`

**功能描述**:
- 应用程序入口类，继承自UIAbility
- 负责应用生命周期管理（onCreate、onDestroy、onWindowStageCreate等）
- 加载主页面HomePage
- 初始化UIContext并存储到AppStorage，供全局使用

**关键代码**:
```typescript
export default class EntryAbility extends UIAbility {
  onWindowStageCreate(windowStage: window.WindowStage): void {
    windowStage.loadContent('pages/HomePage', (err, data) => {
      // 加载主页面
    });
  }
}
```

---

### 3.2 页面层

#### HomePage.ets
**文件路径**: `entry/src/main/ets/pages/HomePage.ets`

**功能描述**:
- 计算器主页面，使用@Entry和@Component装饰器
- 管理输入表达式和计算结果的状态
- 实现键盘按钮的点击处理逻辑
- 支持数字输入、运算符输入、删除、清空、等号计算等功能
- 实现表达式验证和格式化显示（千分位格式化）
- 使用ForEach组件循环渲染键盘按钮

**状态管理**:
- `@State textInput: string`: 输入表达式
- `@State result: string`: 计算结果

**主要方法**:
- `pressKey(item: PressKeysBean)`: 处理按键点击
- `isSymbol(value: string)`: 判断是否为运算符
- `isPercent(value: string)`: 判断是否为百分号
- `isPoint(value: string)`: 判断是否为小数点
- `isNumber(value: string)`: 判断是否为数字
- `isDelete(value: string)`: 判断是否为删除键
- `isClean(value: string)`: 判断是否为清除键
- `isEqual(value: string)`: 判断是否为等号

---

### 3.3 工具类层

#### CalculateUtil.ets
**文件路径**: `entry/src/main/ets/common/util/CalculateUtil.ets`

**功能描述**:
- 核心计算工具类
- 实现表达式解析（使用调度场算法将中缀表达式转为后缀表达式）
- 支持加减乘除四则运算
- 处理百分号运算
- 实现运算符优先级判断
- 支持科学计数法表示大数
- 处理浮点数精度问题

**主要方法**:
- `calculate(str: string)`: 主计算方法，返回计算结果
- `infixToSuffix(infix: string[])`: 中缀表达式转后缀表达式
- `suffixToResult(suffix: string[])`: 后缀表达式计算结果
- `comparePriority(operator1: string, operator2: string)`: 比较运算符优先级
- `scientificCount(num: number)`: 科学计数法转换

**算法说明**:
使用调度场算法（Shunting Yard Algorithm）处理表达式：
1. 将中缀表达式转换为后缀表达式（逆波兰表达式）
2. 计算后缀表达式得到结果

---

#### CheckEmptyUtil.ets
**文件路径**: `entry/src/main/ets/common/util/CheckEmptyUtil.ets`

**功能描述**:
- 空值检查工具类
- 提供对象、字符串、数组的空值判断方法

**主要方法**:
- `isEmpty(obj: Object)`: 判断对象是否为空
- `isStringEmpty(str: string)`: 判断字符串是否为空
- `isArrayEmpty(arr: Array<T>)`: 判断数组是否为空

---

#### Logger.ets
**文件路径**: `entry/src/main/ets/common/util/Logger.ets`

**功能描述**:
- 日志管理工具类
- 封装hilog日志接口
- 提供debug、info、warn、error等日志级别

**主要方法**:
- `debug(message: string)`: 调试日志
- `info(message: string)`: 信息日志
- `warn(message: string)`: 警告日志
- `error(message: string)`: 错误日志

---

### 3.4 常量定义层

#### CommonConstants.ets
**文件路径**: `entry/src/main/ets/common/constants/CommonConstants.ets`

**功能描述**:
- 定义公共常量（运算符、百分号、小数点等）
- 定义Symbol枚举（操作类型）
- 定义Priority枚举（运算符优先级）
- 定义SymbolicEnumeration枚举（运算符符号）

**枚举定义**:
```typescript
export enum Symbol {
  NUMBER = 0,      // 数字
  OPERATOR = 1,    // 运算符
  PERCENT = 2,     // 百分号
  POINT = 3,       // 小数点
  DELETE = 4,      // 删除
  CLEAN = 5,       // 清除
  EQUAL = 6        // 等号
}

export enum Priority {
  LOW = 0,         // 低优先级（加减）
  HIGH = 1         // 高优先级（乘除）
}
```

---

### 3.5 视图模型层

#### PressKeysItem.ets
**文件路径**: `entry/src/main/ets/viewmodel/PressKeysItem.ets`

**功能描述**:
- 定义按键数据模型PressKeysBean
- 包含按键类型、尺寸、值、图标资源等属性

**数据模型**:
```typescript
export class PressKeysBean {
  type: Symbol;           // 按键类型
  width: Length;          // 按键宽度
  height: Length;         // 按键高度
  value: string;          // 按键值
  imgResource: Resource;  // 图标资源
}
```

---

#### PresskeysViewModel.ets
**文件路径**: `entry/src/main/ets/viewmodel/PresskeysViewModel.ets`

**功能描述**:
- 计算器键盘数据模型
- 定义所有按键的布局和属性
- 包含数字键(0-9)、运算符键(+-×÷)、功能键(清除、删除、等号、百分号)

**按键布局**:
```
[清除] [删除] [百分号] [除号]
[  7 ] [  8 ] [   9  ] [乘号]
[  4 ] [  5 ] [   6  ] [减号]
[  1 ] [  2 ] [   3  ] [加号]
[      0      ] [小数点] [等号]
```

---

## 四、配置文件说明

### 4.1 应用配置

#### app.json5
**文件路径**: `AppScope/app.json5`

**配置内容**:
```json5
{
  "app": {
    "bundleName": "com.example.simplecalculator",  // 应用包名
    "vendor": "example",                           // 供应商
    "versionCode": 1000000,                        // 版本号
    "versionName": "1.0.0",                        // 版本名称
    "icon": "$media:app_icon",                     // 应用图标
    "label": "$string:app_name"                    // 应用名称
  }
}
```

---

### 4.2 模块配置

#### module.json5
**文件路径**: `entry/src/main/module.json5`

**配置说明**:
- 定义模块名称为"entry"
- 指定入口能力为"EntryAbility"
- 支持设备类型为"phone"（手机）
- 配置启动窗口图标和背景色

---

### 4.3 构建配置

#### build-profile.json5
**文件路径**: `build-profile.json5`

**配置说明**:
- 目标SDK版本: 6.0.2(22)
- 兼容SDK版本: 5.0.5(17)
- 运行系统: HarmonyOS

---

## 五、资源文件说明

### 5.1 颜色资源

#### color.json
**文件路径**: `entry/src/main/resources/base/element/color.json`

**颜色定义**:
- 输入框背景色: #F5F5F5
- 文字颜色: #182431
- 等号按钮背景色: #007DFF（蓝色）
- 行背景色: #F8F8FF
- 列背景色: #F5F5F5

---

### 5.2 尺寸资源

#### float.json
**文件路径**: `entry/src/main/resources/base/element/float.json`

**尺寸定义**:
- 输入框字体大小: 64fp
- 结果文字大小: 32fp
- 按键宽度: 70vp
- 按键高度: 60vp

---

### 5.3 媒体资源

**文件路径**: `entry/src/main/resources/base/media/`

**图标资源**:
- ic_add.png: 加号图标
- ic_min.png: 减号图标
- ic_mul.png: 乘号图标
- ic_div.png: 除号图标
- ic_clean.png: 清除图标
- ic_del.png: 删除图标
- ic_equ.png: 等号图标
- icon.png: 应用图标

---

### 5.4 多语言资源

**中文资源**: `entry/src/main/resources/zh_CN/`
**英文资源**: `entry/src/main/resources/en_US/`

支持中英文国际化。

---

## 六、核心功能说明

### 6.1 功能列表

1. **基础四则运算**: 支持加(+)、减(-)、乘(×)、除(÷)运算
2. **混合运算**: 支持运算符优先级，乘除优先于加减
3. **百分号运算**: 支持%运算符
4. **小数运算**: 支持小数点输入和运算
5. **实时计算**: 输入时实时显示计算结果
6. **表达式显示**: 显示完整的输入表达式
7. **千分位格式化**: 结果以千分位格式显示
8. **科学计数法**: 大数自动转换为科学计数法
9. **错误处理**: 除零等错误情况显示"error"

---

### 6.2 架构设计

**架构模式**: MVVM (Model-View-ViewModel)

```
┌─────────────────────────────────────────┐
│              View Layer                  │
│  (HomePage.ets - UI组件和交互逻辑)        │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│          ViewModel Layer                 │
│  (PresskeysViewModel.ets - 键盘数据)     │
│  (PressKeysItem.ets - 按键模型)          │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│           Model Layer                    │
│  (CalculateUtil.ets - 计算逻辑)          │
│  (CommonConstants.ets - 常量定义)        │
│  (CheckEmptyUtil.ets - 工具方法)         │
└─────────────────────────────────────────┘
```

**架构说明**:
- **View层**: 负责UI展示和用户交互
- **ViewModel层**: 负责数据模型和业务逻辑
- **Model层**: 负责核心算法和数据处理

---

### 6.3 技术特点

1. **声明式UI**: 使用ArkTS的声明式UI语法
2. **状态管理**: 使用@State装饰器管理组件状态
3. **组件化开发**: 使用自定义组件和内置组件
4. **资源管理**: 使用资源引用($r)实现多语言和主题
5. **算法实现**: 使用调度场算法处理表达式计算

---

## 七、核心算法说明

### 7.1 表达式计算算法

**算法名称**: 调度场算法（Shunting Yard Algorithm）

**算法流程**:
1. **中缀转后缀**: 将中缀表达式转换为后缀表达式（逆波兰表达式）
2. **后缀计算**: 计算后缀表达式得到结果

**示例**:
```
中缀表达式: 3 + 4 × 2
后缀表达式: 3 4 2 × +
计算结果: 11
```

**优势**:
- 支持运算符优先级
- 支持括号（可扩展）
- 计算效率高

---

### 7.2 运算符优先级

| 运算符 | 优先级 |
|--------|--------|
| ×      | 高     |
| ÷      | 高     |
| +      | 低     |
| -      | 低     |

---

## 八、项目亮点

1. **清晰的架构设计**: 采用MVVM架构，职责分离明确
2. **完整的算法实现**: 使用调度场算法处理表达式计算
3. **良好的代码规范**: 遵循ArkTS编码规范
4. **完善的资源管理**: 支持多语言、主题配置
5. **用户体验优化**: 实时计算、千分位格式化、科学计数法
6. **错误处理机制**: 完善的异常处理和错误提示

---

## 九、扩展建议

1. **功能扩展**:
   - 添加括号支持
   - 添加科学计算功能（三角函数、对数等）
   - 添加历史记录功能
   - 添加主题切换功能

2. **性能优化**:
   - 优化表达式解析性能
   - 添加计算结果缓存

3. **用户体验**:
   - 添加按键震动反馈
   - 添加动画效果
   - 支持横屏模式

---

## 十、总结

SimpleCalculator是一个结构清晰、功能完整的HarmonyOS计算器应用示例项目。它展示了：

1. HarmonyOS应用开发的标准目录结构
2. ArkTS声明式UI开发模式
3. MVVM架构设计实践
4. 表达式计算算法实现
5. 资源管理和多语言支持
6. 组件化开发最佳实践

该项目适合作为HarmonyOS应用开发的学习参考，特别是对于理解声明式UI、状态管理、算法实现等方面具有很好的示范作用。

---

**文档生成时间**: 2026-05-12
**文档版本**: v1.0
